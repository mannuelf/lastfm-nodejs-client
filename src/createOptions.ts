export function createOptions(
  method: string,
  user: string,
  period?: string,
  limit?: string,
  page?: string,
  from?: string,
  tag?: string,
  artist?: string,
  album?: string,
  track?: string,
  country?: string,
  autocorrect?: string,
  mbid?: string,
  lang?: string,
  sk?: string,
  token?: string,
  tags?: string,
  timestamp?: string,
) {
  return {
    method,
    page,
    user,
    tag,
    period,
    limit,
    from,
    artist,
    album,
    track,
    country,
    autocorrect,
    mbid,
    lang,
    sk,
    token,
    tags,
    timestamp,
  };
}
